{% set unitName = "Unit Name" %}
{% set facultyName = "Faculty of XYZ" %}
{% set clfFull = true %} {# display the full or fixed width CLF #}
{% set offCanvasDrawer = true %}
{% if offCanvasDrawer %}
  {% set offCanvasDrawerDirection = 'drawer-panel--push-left' %}
  {# expected values are 'drawer-panel--push-left', 'drawer-panel--push-right' 'drawer-panel--cover-left', 'drawer-panel--cover-right' #}
{% endif %}
{% set stickyNavigation = false %}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    {% include "../includes/head.twig" %}
    {% block custom_css %}{% endblock %}
</head>
<body {% if clfFull %}class="full-width"{% endif %}>
  {% if offCanvasDrawer %}
  <div id="pushed-content" class="{{ offCanvasDrawerDirection }} expand">
  {% endif %}
  {% if not clfFull %}
  <div class="container">
  {% endif %}
    {% include "../includes/header.twig" %}
    {% if offCanvasDrawer %}
    {% include "../includes/menu--off-canvas-drawer-trigger.twig" %}
    {% endif %}
    {% if not offCanvasDrawer %}
    {% include "../includes/menu.twig" %}
    {% endif %}
    {% block content %}{% endblock %}
    {% include "../includes/footer.twig" %}
  {% if not clfFull %}
  </div>
  {% endif %}
  {% if offCanvasDrawer %}
  </div>
  <section id="drawer-panel--primary" class="sidebar {{ offCanvasDrawerDirection }}">
    {% include "../includes/menu--off-canvas-drawer.twig" %}
  </section>
  <div id="off-canvas-mask" class="drawer-toggle--primary"></div>
  {% include "../includes/scripts--off-canvas-drawer.twig" %}
  {% endif %}
  {% include "../includes/scripts.twig" %}
  {% if stickyNavigation %}
  {% include "../includes/scripts--sticky-navigation.twig" %}
  {% endif %}
  {% block custom_javascript %}{% endblock %}
</body>
</html>
